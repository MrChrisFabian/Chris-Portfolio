---
interface Props {//aca va typescript
	href: string;
    text: string; //es un atributo de la etiqueta que creo como componente
}
const { href } = Astro.props;
const { text } = Astro.props;
---
<a href={href} class="text-white  hover:underline decoration-4 decoration-orange-600 text-4xl lg:text-2xl" >{text}

